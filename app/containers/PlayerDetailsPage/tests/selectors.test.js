// import { selectPlayerDetailsPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectPlayerDetailsPageDomain();

describe('selectPlayerDetailsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
