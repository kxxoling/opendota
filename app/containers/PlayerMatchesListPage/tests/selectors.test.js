// import { selectPlayerMatchesListPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectPlayerMatchesListPageDomain();

describe('selectPlayerMatchesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
